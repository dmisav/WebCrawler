namespace WebCrawlerConsoleAgent.PageCrawler;

public class PageParser
{
    public HashSet<WebPage> Parse(string pageContent)
    {
        var subLinks = new HashSet<WebPage>();
        return subLinks;
    }
}